---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install python3 and sudo in container
      ansible.builtin.raw: DEBIAN_FRONTEND=noninteractive apt-get update -y && apt-get install -y python3-minimal sudo curl
      changed_when: false
      failed_when: false