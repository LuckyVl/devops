version: '1.0'

volumes:
  test-vol:

networks:
  filatov.v.k-my-netology-hw:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 10.5.0.0/16

services:
  test-service:
    hostname: "test"